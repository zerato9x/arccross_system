# default_era8 — Golbanc homestead vernacular

Era 8 default settlement structures for alpha starters (four nodes adjacent to
Central) and as structure fallback under snow.

Pool id: `default_era8` (`GameEnums.BIOME_PACK_DEFAULT_ERA8`)

**Canonical** home for homestead / barricade / tank structures. Do not mirror
these under `biome_plains/Structures` (removed in asset cleanup).
Terrain still comes from `biome_plains` grass tiles on starter nodes.
